#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies a two integers
 * @a: the first number to be multiplied
 * @b: the second number to be multiplied
 * Auth: Abel Wube
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
