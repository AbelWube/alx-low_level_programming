#include "main.h"
/**
 * _abs - turns the absolute value of the number
 * @n: parameter
 * Auth: Abel Wube
 *
 * Return: result
 */

int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
